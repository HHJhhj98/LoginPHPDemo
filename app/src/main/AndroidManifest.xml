<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.huhuijia.loginphpdemo">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui_login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui_login.ForgetActivity"/>
        <activity android:name=".ui.teacher.TeacherActivity"/>
        <activity android:name=".ui_login.RegisterActivity"/>
        <activity android:name=".ui_student.SelectActivity"/>
        <activity android:name=".ui_student.AddActivity"/>
        <activity android:name=".ui_student.StudentActivity"/>
        <activity android:name=".ui_student.UpdateActivity"/>
        <activity android:name=".ui.teacher.DetailedActivity"/>
    </application>

</manifest>